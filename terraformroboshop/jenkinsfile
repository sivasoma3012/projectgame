pipeline {
    agent {node {label 'terraform'}}

    stages {
        
        stage('Terraform init') {
            steps {
                echo "hi this is new commit"
            }
        }
        stage('Testing') {
            steps {
                echo "testing"
            }
        }
        
    }
}